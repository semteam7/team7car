version: '2'

services:
        sidewaysparker:
                build: .
                network_mode: "host"
                volumes:
                        - ${conf}:/opt/configuration
                ipc: host
                command: /opt/opendlv.scaledcars/bin/miniature/sidewaysparker --freq=$FREQ --cid=111 --verbose=1 -e DISPLAY=$$DISPLAY
                working_dir: /opt/configuration
